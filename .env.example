# ====================================
# 墨焕影视 - Vercel 部署环境变量配置指南
# ====================================
#
# 📋 部署步骤：
# 1. 在 GitHub 上创建仓库并推送代码
# 2. 在 Vercel Dashboard 中导入该仓库
# 3. 在 Vercel Dashboard > Settings > Environment Variables 中添加以下变量
# 4. 也可以直接粘贴 .env 内容到 Vercel 的环境变量输入框
#
# 🔧 构建设置：
# - 构建命令：npm run vercel-build
# - 输出目录：.（留空或填 .）
# - 安装命令：pnpm install
#
# 💾 数据库：
# 在 Vercel Dashboard > Storage 中创建 Postgres 数据库
# 创建后会自动生成 DATABASE_URL 环境变量
#
# ====================================

# 数据库（Vercel Postgres 自动生成，无需手动填写）
DATABASE_URL=

# JWT 密钥（必填，用以下命令生成：node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"）
JWT_SECRET=

# Cloudflare R2 存储（必填）
R2_ENDPOINT=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET_NAME=mhvideo
R2_PUBLIC_URL=
